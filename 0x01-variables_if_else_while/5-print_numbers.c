#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int pi;

	for (pi = 0; pi < 10; pi++)
		printf("%d", pi);
		printf("\n");

	return (0);
}
