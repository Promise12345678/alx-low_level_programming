#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char lo;

	for (lo = 'a'; lo <= 'z'; lo++)
		putchar(low);
	for (lo = 'A'; lo <= 'Z'; lo++)
		putchar(lo);
		putchar('\n');

	return (0);
}
